/**
 * 점유율과 ADR, RevPAR 등 지표 집계를 다룬다. P4 16주차에 구현한다.
 *
 * <p>아직 비어 있는 모듈이다. 이 파일은 Spring Modulith 가 모듈 경계를 인식하도록
 * 자리만 잡아 둔 것이다. 다른 모듈에서 이 패키지의 하위 패키지를 직접 참조하면
 * ModularityTest 가 실패한다.
 */
package com.staysync.analytics;
