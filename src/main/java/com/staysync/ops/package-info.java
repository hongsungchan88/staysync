/**
 * 청소와 점검 태스크, 담당자 배정을 다룬다. P4 15주차에 구현했다.
 *
 * <p>지금 있는 것은 <b>청소 하나</b>다. 체크아웃 이벤트를 듣고 태스크를 만들고,
 * 칸반으로 옮기고, 완료되면 판매 단위가 {@code CLEAN} 으로 돌아온다(계획서 8.6).
 * 정비·점검은 만들 이유가 아직 없어 {@code TaskType} 에 값을 두지 않았다.
 *
 * <p><b>의존 방향은 ops → booking·property·messaging 이다.</b> 다음 체크인은
 * {@code ReservationDirectory}, 판매 단위 상태는 {@code UnitHousekeeping}, 알림은
 * {@code InboxNotes} 로 간다. 어느 모듈의 {@code domain} 도 보지 않는다.
 *
 * <h2>알림을 웹푸시나 알림톡으로 만들지 않았다</h2>
 *
 * <p>계획서 8.6 은 "웹푸시 또는 알림톡"이라고 적었지만 <b>둘 다 이 프로젝트에서
 * 확보할 수 없는 것 위에 있다.</b> 웹푸시는 서비스 워커와 구독 관리와 VAPID 키가
 * 따라오고, 알림톡은 사업자 등록과 발신 프로필 심사가 필요하다.
 *
 * <p>대신 14주차에 만든 인박스에 <b>시스템 메시지</b>로 남긴다. 새 경로를 만들지
 * 않고도 담당자가 볼 곳이 생긴다. 그 메시지는 방향이 {@code INBOUND} 라 발송 작업이
 * 만들어지지 않는다 — 게스트에게는 나가지 않는다.
 *
 * <p>계획서 8.6 의 문구는 실제 서비스를 전제한 것이다. 학기 프로젝트의 범위에서
 * 무엇이 가능한지를 적고 넘어가는 편이 낫다. <b>못 만든 것을 만든 것처럼 적지
 * 않는다</b>(작업지시 12 의 5절 2번).
 *
 * <h2>담당자는 이름 문자열이다</h2>
 *
 * <p>계정을 주려면 역할별 인가가 필요하고 그건 ADR 0004 가 3~5단계로 미뤄 둔 것이다.
 * 지금 열면 인증 설계 전체를 다시 본다. V1 의 {@code assignee_id} 는 그대로 두고
 * V7 이 {@code assignee_name} 을 더했다 — 담당자가 직접 로그인해 완료 처리를 하게
 * 되는 시점에 그 자리가 쓰인다.
 */
package com.staysync.ops;
