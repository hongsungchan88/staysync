# 서버(Linux)로 그대로 올라가는 파일은 LF 로 고정한다. Windows 의 autocrlf 가 CRLF 로
# 바꾸면 bash 가 `set -euo pipefail\r` 에서 죽고 Caddyfile 도 못 읽는다.
deploy/** text eol=lf
*.sh text eol=lf
gradlew text eol=lf

# iCal 픽스처는 실제 발행물 그대로다. CRLF 를 포함해 바이트가 같아야 한다.
src/test/resources/ical/*.ics -text
